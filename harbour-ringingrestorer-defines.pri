# Use this to build a crippled version for Jolla Harbour Store, that won't autostart the daemon but
# will require user to launch the controller application first (which in turns starts the daemon).
DEFINES += RR_JOLLA_STORE_CRIPPLED

